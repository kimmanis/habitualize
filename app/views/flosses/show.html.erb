<% if @floss.skipped == 1 %>
<p>
Really? You aren't going to floss??
</p>

<% else %>
<p>
<%= @user.first_name %>, you did it! Keep on flossing!
</p>
<% end %>

<%= link_to 'Back', root_path, :class=>"button" %>

</br>

<% User.all.each do |user| %>
	<%= image_tag user.image %><%= user.name %>  Score: <%= Floss.where('user = ?', user.id).count %></br>
<% end %>